# Pinned versions matching the verified working environment (Python 3.11, CUDA 12.4).
# Preferred installer is `uv sync` (uses pyproject.toml + uv.lock).
# Plain pip fallback (note: torch/torchvision must come from the cu124 index):
#   pip install --extra-index-url https://download.pytorch.org/whl/cu124 -r requirements.txt

# PDF extraction
docling==2.31.0
docling-core==2.75.0

# CLIP heading detection
torch==2.6.0+cu124
torchvision==0.21.0+cu124
transformers==4.57.6
Pillow==11.3.0

# Token counting
tiktoken==0.13.0

# Progress bar
tqdm==4.67.3

# (Optional) Vietnamese tokenizer - used in upload_qdrant sparse
# underthesea>=6.8.0
